clear
clf
clc

bot = Sawyer(0,0,0);
bot.model.teach();
hold on
%% Robot set up robot
q0 = bot.model.getpos();
qr = [0 -pi -pi/2 pi/2 0 -pi/2 0];

qM = jtraj(q0,qr,30);

bot.model.plot(qM);
bot.model.teach();

%% Robot Trajectory Generation

% Right
q1 = bot.model.getpos();
q2 = 
% Left

% Behind

% Above 

%% Stops on Collision

